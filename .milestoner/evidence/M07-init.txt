M07 AC1/AC2 - what a user sees when init meets a protocol from another run
Captured 2026-08-20T23:40:54Z against the built dist/cli.js, in a temp directory.

== 1. Scaffold run "v04-plugin" (fresh directory) ==
$ node dist/cli.js init --run v04-plugin --milestones 2
20:40:55 prompts/M01.md
20:40:55 prompts/M02.md
20:40:55 config.json
20:40:55 state.json
20:40:55 protocol.md
20:40:55 .gitignore
20:40:55 execution-log.md
20:40:55 decisions.md
20:40:55 supervisor-log.md

== initialized .milestoner/ for run "v04-plugin"

Next:
  1. Edit .milestoner/protocol.md - replace every TODO with this project's rules.
  2. Write .milestoner/prompts/M01.md and friends - objective, tasks, acceptance criteria, exit.
  3. Set the titles in .milestoner/state.json to match.
  4. Point "liveness" in config.json at the paths that prove work is happening
     (source dirs, test-result files, tool logs). The transcript is never one.
  5. milestoner run

To supervise a long run, install the supervisor skill and loop it:
  milestoner skill install
  /loop 10m Use the milestoner-supervisor skill to perform one supervision cycle.

20:40:55 ready
exit=0

== 2. Re-init the same directory for run "v05-debt" ==
$ node dist/cli.js init --run v05-debt --milestones 3 --force
20:40:55 .milestoner/protocol.md names run "v04-plugin", not "v05-debt" - a session would read the old run's rules

  Nothing was scaffolded. Bring the protocol in line yourself - at least the run name in its
  header and the tag line in its Git section (tag `v05-debt-<milestoneId>`) - or delete the file
  to get a fresh template, then run init again.

exit=1

== 3. The old protocol survives untouched, nothing was scaffolded for v05-debt ==
$ head -1 .milestoner/protocol.md
# Execution protocol - run "v04-plugin"
$ grep '"run"' .milestoner/config.json .milestoner/state.json
.milestoner/config.json:  "run": "v04-plugin",
.milestoner/state.json:  "run": "v04-plugin",
$ ls .milestoner/prompts
M01.md
M02.md

== 4. Re-init for the same run "v04-plugin" changes nothing and says nothing about the protocol ==
$ node dist/cli.js init --run v04-plugin --milestones 2 --force
20:40:55 config.json
20:40:55 state.json

== initialized .milestoner/ for run "v04-plugin"

Next:
  1. Edit .milestoner/protocol.md - replace every TODO with this project's rules.
  2. Write .milestoner/prompts/M01.md and friends - objective, tasks, acceptance criteria, exit.
  3. Set the titles in .milestoner/state.json to match.
  4. Point "liveness" in config.json at the paths that prove work is happening
     (source dirs, test-result files, tool logs). The transcript is never one.
  5. milestoner run

To supervise a long run, install the supervisor skill and loop it:
  milestoner skill install
  /loop 10m Use the milestoner-supervisor skill to perform one supervision cycle.

20:40:55 ready
exit=0

== Covering tests (src/commands/init.test.ts, from .milestoner/evidence/M07-test.txt) ==
ok 7 - a fresh scaffold writes a protocol naming the run, tagged without a slash
ok 8 - scaffolding over a protocol naming a different run refuses before writing anything
ok 9 - re-init over the same run's protocol keeps it byte for byte and says nothing about it
ok 10 - a protocol that does not name a run is kept, with a warning that it cannot be checked
Suite: # tests 118, # pass 118, # fail 0.
