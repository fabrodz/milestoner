M07 AC4 - this repository's protocol names v05-debt and the new tag scheme

$ head -1 .milestoner/protocol.md
# Execution protocol - run "v05-debt"

$ git diff .milestoner/protocol.md
warning: in the working copy of '.milestoner/protocol.md', CRLF will be replaced by LF the next time Git touches it
diff --git a/.milestoner/protocol.md b/.milestoner/protocol.md
index 9b8e15c..940ad26 100644
--- a/.milestoner/protocol.md
+++ b/.milestoner/protocol.md
@@ -63,8 +63,9 @@ diagnosis.
 
 - Commit per logical step. Message style: `feat: ...`, `fix: ...`, `docs: ...`, `refactor: ...`,
   lowercase summary, English.
-- On a green milestone gate: final commit plus tag `v04-plugin/<milestoneId>`. Tags are the
-  rollback points for the whole run.
+- On a green milestone gate: final commit plus tag `v05-debt-<milestoneId>`. Tags are the
+  rollback points for the whole run. No slash in tag names: a branch and a tag sharing a name
+  makes `git push origin <name>` ambiguous.
 - **No AI attribution of any kind** in commits: no co-author trailer, no "generated with" line.
 - Never `git push`, never `npm publish`, never rewrite history that is already tagged.
 

$ grep -n 'tag ' .milestoner/protocol.md | head -3
66:- On a green milestone gate: final commit plus tag `v05-debt-<milestoneId>`. Tags are the
67:  rollback points for the whole run. No slash in tag names: a branch and a tag sharing a name
