node_modules/
dist/
*.tsbuildinfo
*.tgz
.vscode/

# Run state: the record of a run belongs to the machine that ran it, not to the repository.
.milestoner/

# Locally installed skills: `milestoner skill install` writes them for the machine, not the repo.
.claude/
